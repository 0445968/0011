// Print styles for financial documents.
// When an element with class "lab-print-area" is printed,
// only the printable document (#printable-document) is shown.

export function printDocument() {
  window.print();
}
